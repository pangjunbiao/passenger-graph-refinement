# Keep the already verified CUDA-matched PyTorch installation:
#   torch 2.7.0+cu128
# Do not use this file to replace PyTorch with a CPU or incompatible build.
numpy>=1.23,<2.0
PyYAML>=6.0,<7.0
pytest>=7.0,<9.0
