# Publication Artifacts

This directory is reserved for small, publication-approved artifacts such as
aggregate tables, figures, or machine-readable summaries that are safe to
redistribute.

Raw datasets, individual human-annotation responses, model checkpoints,
experiment archives, logs, and private intermediate outputs must not be placed
here.

Only artifacts that have been explicitly checked for privacy, licensing,
provenance, and consistency with the manuscript should be committed.